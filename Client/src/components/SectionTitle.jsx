import React from 'react';

const SectionTitle = ({ children }) => {
  return (
    <h2 className="text-[#224229] font-normal text-center mb-10 text-3xl">
      {children}
    </h2>
  );
};

export default SectionTitle;